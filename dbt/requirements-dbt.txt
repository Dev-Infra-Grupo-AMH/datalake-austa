# Ambiente local (dbt fora do Airflow). Alinhado a image/requirements.txt do Airflow.
dbt-core==1.8.0
dbt-spark[PyHive]==1.8.0
python-dotenv>=1.0.0
# Instala sitecustomize no venv (patch Thrift/Glue); evita PYTHONPATH ou scripts locais.
-e ./plugins
